{
  "name": "outtake",
  "version": "2.0.0",
  "private": true,
  "description": "{ OUTTAKE } — multi-artist unreleased music vault. Static frontend (index.html) + DB-backed API.",
  "type": "commonjs",
  "main": "server.js",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "node --test \"test/*.test.js\"",
    "db:migrate": "node src/migrate.js",
    "db:seed": "node scripts/import_catalog.js",
    "db:export": "node scripts/export_catalog.js"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}